/**
 * @file
 * voltahall behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.voltahall = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
